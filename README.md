# SMS-Spam-Detection-Model
This project implements a **machine learning model** to classify SMS messages as **spam or ham (not spam)**.   It demonstrates text preprocessing, feature extraction, and model evaluation using popular Python libraries.
